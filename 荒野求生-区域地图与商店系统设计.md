# 荒野求生 · 区域地图与商店系统设计

> 目标：把「点击进入的区域地图」做得**更真实、更大**，并在其中建立一套**有血有肉的商店系统**。
> 状态：以下设计**已全部实现**并打包上线（回归 9/9 通过）。

---

## 一、现状诊断：为什么"不真实"

### 1. 地形是白噪声，不是地貌（根因）

原 `makeZone` 逐格独立随机：

```js
var t = (rnd() < 0.26) ? variants[...] : R.terrain;   // 每格独立掷骰
```

格子之间**零空间相关性**——森林格旁边随机接湖泊、再接洞穴。真实景观是森林成片、水域连湖、山脉成岭。
结果就是"emoji 棋盘"，玩家看到的是噪点而非地貌。

### 2. 只有 8×8 = 64 格

面积太小，走两步就到头，没有"区域纵深"的感觉。

### 3. 渲染是"格子里放 emoji"

所有已探索格底色一致，仅靠一个 emoji 区分地形，缺乏高程、纹理、光影。

### 4. 商店：`max` 库存字段定义了却从未使用

`SHOP_STOCK` 里每项都有 `max`（库存上限），但代码里**没有任何一处读取它**——商店实际上是**无限量供应**。
此外：无补货、无价格波动、无批量出售，且**只有营地那一格**能进商店。

---

## 二、区域地图设计

### 2.1 连贯地貌生成（核心改造）

用**值噪声 + 分形叠加（fbm）**生成两张连续场，再按规则翻译成地形：

| 场 | 作用 | 参数 |
|---|---|---|
| `elev` 高程 | 决定低洼/中地/高地 | scale 4.2，2 个八度 |
| `moist` 湿度 | 决定湿润（林/沼）或干燥（平原） | scale 3.4，2 个八度 |

**分类规则**（保证只出现该区域"可能存在"的地形，从而保留区域身份）：

| 条件 | 结果 |
|---|---|
| `elev < 0.30` | 低洼 → 水系（河流 / 沼泽 / 沙滩） |
| `elev > 0.72` | 高地 → 山岭 / 火山 / 洞窟 / 遗迹 |
| `moist > 0.58` | 湿润中地 → 森林 / 沼泽 |
| `moist < 0.38` | 干燥中地 → 平原 / 沙滩 |
| 其余 | 区域主地形 |

两个关键实现细节（踩过的坑）：

1. **必须归一化到满量程**：fbm 叠加后输出集中在 0.5 附近呈钟形，若不归一化，
   `<0.28` / `>0.70` 的阈值几乎永不触发，地形会退化成单一主地形（首版海滩区 144 格里**0 格水**）。
   新增 `normalizeField()` 按 min/max 拉伸到 0~1。
2. **同一带内要轮换类型**：若只取"第一个可用类型"，变体表窄的区域会整片塌掉
   （首版火山区 **144/144 全 volcano**）。改为 `pickFrom()` 在可用类型里按湿度轮换。

**效果实测**（相邻同地形比例，越高越连贯；括号内为同等分布下纯随机的理论值）：

| 区域 | 连贯度 | 地形分布 |
|---|---|---|
| 海滩 | 72%（随机约 44%） | 海滩 86 · 河流 28 · 平原 30 |
| 雨林 | 59%（随机约 33%） | 森林 55 · 沼泽 48 · 平原 41 |
| 湖泊 | 69% | 河流 60 · 平原 47 · 沼泽 37 |
| 山地 | 78% | 山地 101 · 平原 38 · 洞窟 5 |
| 火山 | 89% | 火山 128 · 山地 14 · 洞窟 2 |
| 沼泽 | 89% | 沼泽 124 · 河流 20 |

### 2.2 扩容：8×8 → 12×12

面积 **2.25 倍**（64 → 144 格）。为抵消探索能耗上涨，
把「🔭 探索」的揭示半径从 **1 提到 2**（单次 3×3=9 格 → 5×5=25 格），
配合移动时自动揭示（半径 1），整体探索节奏保持不变。

### 2.3 渲染：俯视地形图

- **地形本色 + 高程明暗**：按 `elev` 调整明度（低洼压暗、高地提亮），产生立体感。
- **地形纹理**（`TERRAIN_TEX`，半透明 CSS 渐变叠在地形色上）：
  水波、沙滩纹、树冠斑、山脊斜光、沼泽斑驳、洞穴暗角、火山岩浆光、遗迹砖纹、草原条纹。
- **弱化网格线**：相邻同地形自然融合，不再是棋盘。
- **玩家令牌**：绿色圆形白边脉冲标记 🧍（替换原来的绿方块）。
- **迷雾 / 边界**：迷雾斜纹暗化，可推进边界金色虚线闪烁。
- **商人帐篷**：⛺ 角标 + 金色光环，探索到才显形。

---

## 三、商店系统设计

主题：**流浪商人**——固定营地 + 散落各区的帐篷，让"找商店"本身成为探索动机。

### 3.1 商人分布

| 类型 | 位置 | 说明 |
|---|---|---|
| 营地商店 | 营地 POI 中心格 | **必定存在**，主商店 |
| 流浪帐篷 | 其余区域随机格（60% 概率） | 探索奖励；地图上 ⛺ 标记 |

营地是保底入口，帐篷是惊喜——不会因为找不到商店而卡死。

### 3.2 商品与库存

- **限量库存**：`SHOP_STOCK.max` 终于生效，买入即扣，售罄显示"售罄"。
- **每日补货**：每天（或切换区域）重掷库存，UI 明示"每日补货一次"。
- **价格波动**：每日 **±15%** 种子化浮动，同一物品不同天价格不同。
- **区域特色货**：按所在地形进货——洞穴区进石头/铁/铜，火山区进铁/银/夜鳞，
  特产**多进 2 件**且可能上架基础货里没有的品类。
- **每日稀有品**：每天随机 1 件（药品 / 银矿 / 遗迹石板 / 夜鳞 / 铜矿），**仅 1 件卖完即止**。
- **买卖价差**：卖出价 ≈ 买入价的一半（`sellBase 6 × 重量`），保持"卖血亏"的压力。

### 3.3 交互

- 走到商人格 → 地点面板出现「⛺ 进入商人帐篷 / 打开商店」。
- 商店面板：稀有品独立金色行 → 常规货（显示**剩余库存**）→ 出售（显示单价）。
- **批量出售**：持有数量 >1 时多一个「全卖(N)」按钮，省去反复点击。

---

## 四、已实现清单

**区域地图**
- [x] `makeNoiseField` / `fbmField` / `normalizeField` / `zoneTerrainAt` 连贯地貌生成
- [x] 网格 8×8 → 12×12；探索半径 1 → 2
- [x] `TERRAIN_TEX` 11 种地形纹理 + `shadeHex` 高程明暗
- [x] 玩家圆形脉冲令牌；弱网格线；商人 ⛺ 标记
- [x] 每格记录 `t.e`（高程），供渲染与后续扩展使用

**商店**
- [x] `REGION_GOODS` 区域特色货、`EXTRA_PRICES` 补充定价、`RARE_POOL` 稀有品池
- [x] `shopCatalog()` 目录（基础货 + 区域特色）
- [x] `ensureShop()` 每日/换区补货、库存、±15% 价格、稀有品
- [x] `buyItem` / `buyRare` / `sellItem` / **`sellAll` 批量出售**
- [x] `renderShop` 显示库存与稀有品行；`renderSite` 商人格可进入
- [x] 营地必带商店；其余区域 60% 概率带帐篷

**验证**
- [x] `node --check` 语法通过
- [x] 无头回归 **9/9**（跨 3 难度完整对局，零运行期错误）
- [x] 定向用例：网格 144 格、纹理 144 格、商人格标记、营地为 POI、买入扣库存、全卖结算、洞穴区上架石头

---

## 五、平衡与风险说明

- **探索能耗**：地图扩大 2.25 倍，已用揭示半径补偿；回归显示存活天数未出现塌方
  （普通 21–36 天，与改造前 19–24 天同量级）。
- **旧存档兼容**：新增字段 `t.e`（高程）、`z.shopIdx`（商人格）、`G.shop`（商店状态）
  均为可选，旧档读取时按缺省处理（`ensureShop` 会自动补货），不会崩档。
- **区域特色货定价**：为避免依赖资源重量的估算偏差，额外品类使用显式 `EXTRA_PRICES` 定价表。

---

## 六、可选的后续方向（未做）

1. **商人好感 / 议价**：交易次数累积给折扣，与阿海好感并行。
2. **委托收购**：商人发布"今日高价收购某物资"，制造市场波动与决策。
3. **区域地图等高线**：在高程场基础上叠等高线，进一步强化地形真实感。
4. **商人移动**：每隔几天帐篷在已探索格中换位，强化"流浪"主题（当前为固定位置）。
