# 《荒野求生》数值外置 + 蒙特卡洛平衡报告

> 生成时间：2026-09-13
> 工具：`tools/sim.js`（无头模拟器）、`tools/extract-script.js`（内联脚本抽取）
> 数据来源：自动化机器人批量跑局，**不含真人试玩数据**

---

## 1. 做了什么

### 1.1 数值外置（BAL 平衡表）

原先所有消耗 / 伤害 / 成长 / 节奏数值都硬编码在函数里，改一个数要翻遍 3000 行。现已全部抽到 `survival.html` 顶部的 **`BAL` 配置表**，分 15 个分组：

| 分组 | 覆盖内容 |
|---|---|
| `time` | 时段时长、每天时段数、寻路步进、体温上下限 |
| `drain` | 饥饿/口渴消耗、庇护所减耗、体温公式、失温/中暑惩罚、自然回血 |
| `dot` | 中毒/流血/灼烧每回合伤害与持续回合 |
| `gather` | 采集体力与产量、富集加成、冷却、科技点 |
| `explore` / `move` | 探索与移动的体力消耗、推进迷雾加成 |
| `ambush` | 夜间/血月/天气遇敌率与减免系数 |
| `combat` | 暴击、怒气、特殊攻击、防御减伤、重击、逃跑、精英怪 |
| `death` | 战败后残留生命、资源损失比例 |
| `progress` | 血月/灾变/季节周期、敌人阶数、科技点奖励、事件概率、**事件链权重** |
| `rest` / `food` / `medicine` | 休息恢复、食物数值、药品治疗 |
| `craft` / `economy` / `build` / `backpack` | 制作成长、售价公式、建筑产出、负重上限 |

**抽取原则：默认值 = 抽取前的硬编码值**，因此抽取本身不改变任何行为（已用回归验证）。

### 1.2 蒙特卡洛工具

`tools/sim.js` 是一个可复用的无头模拟器：伪造 DOM/localStorage/WebAudio，把游戏脚本装载进 `vm` 沙箱，用策略机器人驱动完整对局。

```bash
node tools/sim.js --regress            # 回归：3 难度 × 3 局，校验零运行期错误 + 不变量
node tools/sim.js --games 30           # 蒙特卡洛：每难度 30 局，输出存活/战斗/任务分布
node tools/sim.js --sweep --games 24   # 参数扫描：对比 5 套数值方案的存活曲线
```

不变量校验：负重不越界、生命不溢出、体力在 0–100。

---

## 2. 存活曲线（每难度 20–24 局）

| 难度 | 中位存活 | 平均存活 | 平均击杀 | 平均任务进度 | 通关率 |
|---|---|---|---|---|---|
| 轻松 | 23 天 | 23.6 天 | 11.8 | 1.9 / 5 | 0% |
| 普通 | 23 天 | 23.3 天 | 11.8 | 1.9 / 5 | 0% |
| 硬核 | 10 天 | 8.8 天 | 4.8 | 1.4 / 5 | 0% |

死亡原因构成（普通难度）：力竭（饥饿/失温/中毒）约 65%、灾变约 15%、战斗约 20%。
硬核难度则相反：约 80% 死于战斗，符合"硬核=战斗压力"的定位。

---

## 3. 扫描结论：哪个杠杆真的有用

对同一组种子跑 5 套方案（每方案 3 难度 × 24 局）：

| 方案 | 轻松 | 普通 | 硬核 | 结论 |
|---|---|---|---|---|
| 基线 | 22.4 | 18.9 | 7.4 | — |
| 消耗 −25%（饥饿3→2、口渴4→3） | 18.5 | 19.9 | 7.0 | **几乎无效**（噪声范围内） |
| 敌人阶数放缓（day/4 → day/6） | 24.3 | 21.5 | 9.8 | **有效**，硬核 +32% |
| 回血翻倍 | 24.1 | 23.2 | 7.4 | **有效**，普通 +23% |

**关键发现：死亡主要来自战斗与战斗后的残血，而不是温饱。** 单纯降低饥饿/口渴消耗并不能延长寿命——这与"生存游戏的难点是找吃的"这个直觉相反，是本轮最重要的一条数据。

---

## 4. 本轮据此修掉的真实问题

| # | 问题 | 证据 | 修复 |
|---|---|---|---|
| 1 | **轻松难度开局即超重**：起始物资合计 20.2 / 上限 20，一开局就"背包已满" | 不变量校验报 `overweight 20.2/20`，三局全中 | 起始石头 4→3、清水 5→4（合计 17.8） |
| 2 | **第 5 天血月可能刷出棕熊**（110 HP / 18 攻），而玩家此时只有 10 攻 0 防——数学上打不过 | 硬核难度 5 天内战死占比最高 | 血月敌人阶数改为与常规同步（`bloodMoonTierBonus=0`），改为给血月敌人 +25% 属性（`bloodStatMul`）保留压迫感 |
| 3 | **战败死亡螺旋**：非硬核战败后固定回 1 血，下一次结算必死 | "力竭而亡"占普通难度死亡 65% | 改为回到 15% 最大生命（下限 12） |
| 4 | **精英怪开局就可能出现**：第 2 天遇到 1.95 倍属性精英几乎必死 | 早期战死记录 | 精英最早从第 6 天出现（`eliteMinDay=6`） |
| 5 | **中期曲线偏陡** | 扫描数据 | 敌人阶数 `day/4 → day/5`；白天自然回血 2→3、每层庇护所 1→2；休息治疗 4→6 / 每层 2→3 |

修复后（同一组种子、同策略）：**普通 18.9 → 23.3 天（+23%）、硬核 7.4 → 8.8 天（+19%）**，回归 9/9 通过、零运行期错误。

---

## 5. 仍未解决的最大问题（需要设计决策）

**通关率 0%，且任务进度中位数只有 1.9 / 5。**

机器人平均活到第 20 多天，但完整通关需要：完成 5 段任务链（含"击杀**棕熊**"这一随机遭遇型任务、"收集 2 块遗迹石板"）＋ 40 木 / 20 纤维 / 15 铁 / 10 铜。两条硬门槛：

1. **任务 3「除掉威胁」要求击杀特定物种（棕熊）**，而棕熊只是随机遇敌池的一员，且第 8 天后才出现——没有任何指引告诉玩家去哪里找。它是当前最卡的死结（进度中位数正好停在 1.9，即卡在第 2 段之后）。
2. **终局材料量与玩家实际生存时长不匹配**：凑齐材料需要稳定活到 30 天以上，而中位存活是 23 天。

这两条都属于**内容与数值设计决策**（改任务目标 / 改材料门槛 / 加指引），不是纯调参能解决的，因此本轮**只暴露问题、暂不改动**。建议下一步二选一：

- **方案 A（降门槛）**：信号塔材料砍到 30/15/10/6，任务 3 改为"击杀任意 3 只 Tier≥2 的野兽"。
- **方案 B（保门槛、加指引）**：保留现数值，但给棕熊一个固定刷新点（山地/洞穴标记"熊窟"），并把任务 3 的目标写进任务描述。

---

## 6. 怎么复现 / 继续调参

1. 改 `survival.html` 里的 `BAL`（只改这一张表）。
2. `node tools/sim.js --regress` 确认不崩。
3. `node tools/sim.js --sweep --games 30` 对比改动前后的存活曲线。
4. 想做一次性实验不必改文件：`runGame({ overrides: { drain:{hunger:2} } })` 可在运行时覆写任意 BAL 字段。

> 注：本报告全部结论基于机器人模拟。机器人不会像真人一样主动规避血月、囤药、规划路线，因此**真实玩家的存活天数应显著高于本报告数值**；报告的价值在于"改动前后的相对比较"，而非绝对值。
